query CurrentUser {
  currentUser {
    uuid
    representedInstitutions {
      edges {
        node {
          uuid
          name
        }
        role
        dataSigningPermission
      }
    }
  }
}