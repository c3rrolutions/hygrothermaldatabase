query Database(
    $id: Uuid!
) {
    database(id: $id) {
        uuid
        name
        description
        locator
        verificationState
        verificationCode
        operator {
            node {
                uuid
            }
        }
        canCurrentUserUpdateNode
        canCurrentUserVerifyNode
    }
}