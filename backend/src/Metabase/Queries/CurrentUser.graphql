query CurrentUser {
  currentUser {
    id
    uuid
    representedInstitutions {
      edges {
        node {
          id
          uuid
          name
        }
        role
        dataSigningPermission
      }
    }
  }
}